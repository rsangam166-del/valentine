<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Valentine?</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      background: radial-gradient(circle, #ff4d6d, #590d22);
      font-family: Arial, sans-serif;
      overflow: hidden;
      color: white;
      text-align: center;
    }

    h1 {
      margin-bottom: 8px;
    }

    p {
      font-size: 18px;
      margin-bottom: 30px;
      padding: 0 10px;
    }

    .btns {
      position: relative;
      width: 300px;
      height: 200px;
    }

    button {
      padding: 14px 32px;
      font-size: 18px;
      border: none;
      border-radius: 30px;
      cursor: pointer;
      position: absolute;
    }

    #yes {
      background: #2ecc71;
      color: white;
      left: 40px;
      top: 110px;
    }

    #no {
      background: #e63946;
      color: white;
      right: 40px;
      top: 110px;
    }

    .heart {
      position: fixed;
      font-size: 24px;
      animation: float 3s linear forwards;
    }

    @keyframes float {
      from {
        transform: translateY(0);
        opacity: 1;
      }
      to {
        transform: translateY(-300px);
        opacity: 0;
      }
    }
  </style>
</head>
<body>

  <h1>Anya ❤️</h1>
  <p id="text">Will you be my Valentine?</p>

  <div class="btns">
    <button id="yes" onclick="yesClick()">Yes 💖</button>
    <button id="no" onclick="runNo()">No 💔</button>
  </div>

<script>
  let lines = [
    "Arre soch ke bolo 😏",
    "Itni jaldi No? 🥺",
    "Dil sach me toot raha hai 💔",
    "Anya pleaseeee 😭",
    "Ek chance to banta hai 😌",
    "Itna bhi bhaav mat khao 😜",
    "Maine chocolate bhi sochi hai 🍫",
    "Last last last chance 😳",
    "Ab No dabaya to phone ro dega 📱💔",
    "Okay maan lo na 😇"
  ];

  let index = 0;

  function runNo() {
    let no = document.getElementById("no");

    let x = Math.random() * 200;
    let y = Math.random() * 130;

    no.style.left = x + "px";
    no.style.top = y + "px";

    document.getElementById("text").innerText = lines[index];
    index = (index + 1) % lines.length;

    if (navigator.vibrate) navigator.vibrate(100);
  }

  function yesClick() {
    document.getElementById("text").innerText =
      "Hehe 😄 chal ab ek chumma de 😘";

    if (navigator.vibrate) navigator.vibrate([200, 100, 200]);

    for (let i = 0; i < 35; i++) {
      createHeart();
    }
  }

  function createHeart() {
    let heart = document.createElement("div");
    heart.className = "heart";
    heart.innerHTML = "❤️";
    heart.style.left = Math.random() * 100 + "vw";
    heart.style.bottom = "0px";
    document.body.appendChild(heart);

    setTimeout(() => {
      heart.remove();
    }, 3000);
  }
</script>

</body>
</html>
